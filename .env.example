# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

GOOGLE_PLACES_API_KEY=
# OAuth client JSON downloaded from Google Cloud Console (Desktop app type) — see docs/setup.md §4
GOOGLE_SHEETS_CREDENTIALS_JSON=config/credentials.json
GOOGLE_SHEETS_SPREADSHEET_ID=

# Stretch goal only (AI classification, PRD §17) — not needed for the Pilot Slice core pipeline
OPENROUTER_API_KEY=
