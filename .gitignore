# Secrets — never commit these
.env
*.env
config/credentials*.json
**/service_account*.json

# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/

# Local-only data (real leads must never live in git — Sheets/Supabase is the store)
/data/*
!/data/.gitkeep

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
